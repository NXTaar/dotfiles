local palette = require('nxtaar.editor.statusline.constants.palette')
local theme_utils = require('nxtaar.editor.statusline.utils.theme')

return theme_utils.extract_theme(palette.palette_extract_info)
