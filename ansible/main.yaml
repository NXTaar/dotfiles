---
- name: NXTaar Dev Environment
  hosts: localhost
  connection: local

  pre_tasks:
  - ansible.builtin.import_tasks: pre_tasks/detect_wsl.yaml
  - ansible.builtin.import_tasks: pre_tasks/dotfiles_root.yaml

  roles:
    - zsh
    - lazygit
    - rust
    - nvim
    - wezterm
    - node
